<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.digidine">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ranchers_contactus"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ranchers_findus"
            android:label="@string/title_activity_ranchers_findus" />
        <activity android:name=".ranchers_menu" />
        <activity android:name=".ranchers_home_page" />
        <activity
            android:name=".optp_findus"
            android:label="@string/title_activity_optp_findus" />
        <activity android:name=".optp_contactus" />
        <activity android:name=".optp_menu" />
        <activity android:name=".optp_home_page" />
        <activity android:name=".mcdonalds_contactus" />
        <activity
            android:name=".mcdonalds_findus"
            android:label="@string/title_activity_mcdonalds_findus" />
        <activity android:name=".mcdonalds_menu" />
        <activity android:name=".kfc_contactus" />
        <activity android:name=".mcdonalds_home_page" />
        <activity android:name=".kfc_menu" />
        <activity
            android:name=".KFC_FindUs_Activity"
            android:label="@string/title_activity_find_us_" />
        <activity android:name=".MainActivity" />
        <activity android:name=".KFC_Home_Page" />
        <activity android:name=".ranchers" />
        <activity android:name=".OPTP" />
        <activity android:name=".mcdonalds" />
        <activity android:name=".KFC" />
        <activity android:name=".Restaurant_Signup" />
        <activity android:name=".Restaurant_Login" />
        <activity android:name=".Restaurant_Verification" />
        <activity android:name=".Welcome1" />
        <activity
            android:name=".Customer"
            android:label="@string/title_activity_customer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>